meta {
  name: tenant
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/tenants
  body: json
  auth: inherit
}

body:json {
  {"name":"","slug":""}
}

settings {
  encodeUrl: true
  timeout: 0
}
